import React from 'react';
import { View, Text } from 'react-native';

export default function TicketsScreen() {
  return (
    <View className="flex-1 items-center justify-center bg-slate-50 dark:bg-dark-900">
      <Text className="text-xl font-bold text-slate-900 dark:text-white">Tickets Screen</Text>
    </View>
  );
}